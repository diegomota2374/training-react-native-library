import { ThemedText } from "../ThemedText";
import { ThemedView } from "../ThemedView";

const BookList = () => {
  return (
    <ThemedView>
      <ThemedText type="default">book List</ThemedText>
    </ThemedView>
  );
};
export default BookList;
